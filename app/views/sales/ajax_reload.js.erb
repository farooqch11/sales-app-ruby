
// Shared options
$('.added_items').html("<%= escape_javascript render('sales/shared/added_items') %>");
$('.sale_totals').html("<%= escape_javascript render('sales/shared/sale_totals') %>");
$('.sale_payments').html("<%= escape_javascript render('sales/shared/sale_payments') %>");
$('.added_customer').html("<%= escape_javascript render('sales/shared/added_customer') %>");
$('#makePayment .modal-body').html("<%= escape_javascript render('sales/shared/payment') %>");
//$('#customItem .modal-body').html("<%#= escape_javascript render('sales/shared/create_custom_item') %>");
//$('#customCustomer .modal-body').html("<%#= escape_javascript render('sales/shared/create_custom_customer') %>");

$('.print_totals').html("<%= escape_javascript render('sales/shared/invoice_totals') %>");

//$('.searched_customers').html("<%#= escape_javascript render('sales/shared/searched_customers' , available_customers: @available_customers) %>");

$('.btn-charge').text("<%= value_in_currency(@sale.total_amount) %>");
$('.edit_sale').show();
 $('.item-eq').equalHeights();
$('.task-card-photo').equalHeights();
$('.item-img').equalHeights();
$('.task-card-title').equalHeights();
$('#h1-total-price').text("<%= number_to_currency(@sale.remaining_balance, unit: current_company.currency) %>");
$(document).ready(function () {
    $('#horizontalTab').easyResponsiveTabs({
        type: 'default', //Types: default, vertical, accordion
        width: 'auto', //auto or any width like 600px
        fit: true   // 100% fit in a container
    });
});
<%if flash[:alert].present? || flash[:toastr].present? || flash[:error].present? || flash[:errors].present? || flash[:success].present? || flash[:notice].present? || flash[:info].present?%>

<%if flash[:alert].present?%>
flash_alert("<%= flash[:alert] %>");
<%end%>

<%if flash[:error].present?%>
flash_error("<%= flash[:error] %>");
<%end%>

<%if flash[:errors].present?%>
<%flash[:errors].each do |e|%>
flash_error("<%= e %>");
<%end%>
<%end%>

<%if flash[:success].present?%>
flash_success("<%= flash[:success] %>");
<%end%>

<%if flash[:notice].present?%>
flash_notice("<%= flash[:notice] %>");
<%end%>

<%if flash[:info].present?%>
flash_info("<%= flash[:info] %>");
<%end%>
<%end%>